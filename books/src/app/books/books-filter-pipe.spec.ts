import { BooksFilterPipe } from './books-filter-pipe';

describe('BooksFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BooksFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
